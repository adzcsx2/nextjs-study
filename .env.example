# Next.js 环境变量配置示例
# 使用说明：
# 1. 复制此文件并重命名为 .env.local（开发环境使用）
# 2. 根据你的实际环境修改下面的配置值
# 3. .env.local 文件会被 .gitignore 忽略，不会上传到代码仓库
# 4. 多环境部署说明：
#    - 开发版：使用 .env.local 
#    - 测试版：使用 .env.test（端口 3001）
#    - 生产版：使用 .env.production（端口 3000）
#
# 构建命令：
# npm run dev                    # 开发模式
# npm run build:test             # 构建测试版
# npm run build:production       # 构建生产版
# npm run start:test             # 启动测试版
# npm run start:production       # 启动生产版

# ==================== API 配置 ====================
# API 基础地址 - 用于接口代理和请求
# 开发环境建议使用 Mock API 或本地服务器
# 测试环境请替换为测试服务器地址
# 生产环境请替换为实际的生产 API 地址
# 示例：
#   开发版：https://mock.apifox.cn/m1/2398938-0-default
#   测试版：https://test-api.example.com  
#   生产版：https://api.example.com
NEXT_PUBLIC_BASE_API=https://mock.apifox.cn/m1/2398938-0-default

# ==================== 应用配置 ====================
# 应用标题 - 显示在浏览器标题栏和页面中
# 不同环境建议使用不同标题以便区分：
#   开发版：应用名称(开发版)
#   测试版：应用名称(测试版)  
#   生产版：应用名称
NEXT_PUBLIC_APP_TITLE=Next.js 应用

# 应用版本标识
# 建议根据环境设置：dev, test, production
NEXT_PUBLIC_APP_VERSION=dev

# 环境名称标识  
# 用于代码中的环境判断：development, test, production
NEXT_PUBLIC_ENV_NAME=development

# ==================== 国际化配置 ====================
# 默认语言 (zh: 中文, en: 英文)
NEXT_PUBLIC_DEFAULT_LANGUAGE=zh

# 支持的语言列表（逗号分隔）
NEXT_PUBLIC_SUPPORTED_LANGUAGES=zh,en

# ==================== 开发配置 ====================
# 是否启用 Mock 数据 (true/false)
# 环境建议：
#   开发版：true（使用 Mock 数据便于开发）
#   测试版：false（连接真实测试接口） 
#   生产版：false（连接生产接口）
NEXT_PUBLIC_USE_MOCK=true

# 调试模式 (true/false)
# 环境建议：
#   开发版：true（显示详细日志）
#   测试版：true（保留调试信息）
#   生产版：false（关闭调试减少性能开销）
NEXT_PUBLIC_DEBUG=true

# 日志级别 (debug/info/warn/error)
# 环境建议：
#   开发版：debug（显示所有日志）
#   测试版：info（显示重要信息）
#   生产版：error（只显示错误）
NEXT_PUBLIC_LOG_LEVEL=debug

# ==================== 服务器配置 ====================
# 应用运行端口
# 端口分配：
#   开发版：3000（默认开发端口）
#   测试版：3001（避免与开发版冲突）
#   生产版：3000（标准 HTTP 服务端口）
PORT=3000


# ==================== 数据库配置（可选）====================
# 如果项目需要数据库连接，可以取消注释并配置
# DATABASE_URL=your_database_connection_string
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=your_database_name
# DB_USER=your_username
# DB_PASSWORD=your_password

# ==================== 第三方服务配置（可选）====================
# 如果集成了第三方服务，可以取消注释并配置对应的 API Key
# GOOGLE_ANALYTICS_ID=your_ga_id
# SENTRY_DSN=your_sentry_dsn
# STRIPE_SECRET_KEY=your_stripe_secret_key
